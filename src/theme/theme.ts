import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  borderRadius: '4px',

  colors: {
    primary: '#fff',
    primaryText: '#000',
    secondary: '#fff',
    secondaryText: '#000',
    body: '#000',
    bodyText: '#000',
  },
};

export default theme;
